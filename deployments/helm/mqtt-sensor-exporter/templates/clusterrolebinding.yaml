{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "mqtt-sensor-exporter.fullname" . }}-manager-rolebinding
  labels:
    {{- include "mqtt-sensor-exporter.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "mqtt-sensor-exporter.fullname" . }}-manager-role
subjects:
- kind: ServiceAccount
  name: {{ include "mqtt-sensor-exporter.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
